Review Companion's current schedules and how well it is helping the user. Propose improvements only when the evidence warrants them; do not apply changes. This is a usage review, not a code review or a new onboarding conversation.

Ask questions and request approval through ordinary chat messages, never special question tools such as `ask_user` or `ask_user_batch`, forms, or modal dialogs. The user can answer when they have time. When an answer is needed, ask one focused question, keep it pending in authorized notes or the current conversation, and end the turn. Do not chase an answer with timeouts, polling, scheduled reminders, or repeated questions. Silence is neither approval nor a permanent decline; leave the dependent action untouched until the user replies.

Read ~/.companion-schedules.md in full if it exists, list current-session scheduler tasks when available, and inspect relevant onboarding/task notes under ~/.companion-notes/ plus recent relevant entries in ~/.companion-reports-updates.md. Use only approved sources and context. Missing artifacts, unavailable tools, or incomplete checks are gaps to report, not permission to install anything, recreate schedules, or inspect more accounts or session archives.

Keep the review inexpensive: start with the existing notes and a bounded recent sample rather than rerunning every checklist or rereading entire histories. Use a cheap read-collect helper only when useful and authorized for those sources. If this is a scheduled review, follow its current registry entry and access boundaries. If a read-only check repeatedly fails in the same way, stop that check for this run, record the gap and useful next step in its task notes, and respect the agreed run limit instead of expanding the search.

Do not treat a time-limited plan as a standing preference. Prefer explicit user corrections over older factual notes; if conflicting facts or unclear timing would materially change a recommendation, ask one focused question rather than guess. This does not change registry authority or permissions.

Assess whether schedules still support the user's stated routines, goals, and desired outcomes, not whether they merely fire or produce reports. Look for duplicate effort, missed or failed checks, noisy or stale recommendations, inconvenient cadence, and opportunities to simplify. Quiet preventative checks can still be useful; absence of feedback does not prove failure or success.

Offer only a few worthwhile changes, each with an observed reason, supporting evidence, expected user benefit, and the smallest proposed adjustment. Keeping things as they are is a valid outcome. Do not invent improvements, usefulness scores, or new infrastructure. Ask for approval before changing any schedule, cadence, registry entry, preference, permission, or integration; do not resume stopped tasks.

Append new, useful review findings to ~/.companion-reports-updates.md using a real append operation, never a read-modify-write replacement. Give the complete entry a timestamp with timezone, task name, report label, title, and stable publication ID; include concise evidence, gaps, and proposals. Compare prior entries before publishing, check the ID before retrying an uncertain append, and confirm the write. Never overwrite, truncate, rotate, or rewrite the file, repeat unchanged proposals, or echo the full entry in chat. Do not open, close, or restart Herdr panes; an existing tail -f pane follows this same file.

For a manual review with no worthwhile changes, reply briefly without appending a no-news report. For a scheduled review, keep no-change outcomes quiet and record a concise baseline, checked sources, and unresolved gaps in the relevant task notes; a partial review must preserve the last successful checkpoint. These notes and warranted report entries are the only writes allowed by the review itself.
